<p>
  <b>Kontakt:</b>
   <%=h  @talk.contact.first_name if @talk.contact   %> <%=h @talk.contact.last_name if @talk.contact  %> 
   
</p>
<p>
	<b>stav: </b><%=h @talk.contact.state if @talk.contact  %> 
</p>
<p>
  <b>Kdy zavolat:</b>
  <%=h   @talk.call_when_time.strftime("%d-%m-%Y %H.%M| Den: %a | Týden:%W") if @talk.call_when_time %>
</p>

<p>
  <b>Hovor skončil:</b>
  <%=h "Ano" if @talk.finished %>
</p>

<p>
  <b>Poradna:</b>
  <%=h "Ano" if @talk.poradna  %>
</p>

<p>
  <b>Poznamka:</b>
  <%=h @talk.poznamka %>
</p>

<%= link_to 'Editovat', edit_talk_path(@talk) %> |
<%= link_to "Nový hovor" , {:controller => "talks", :action => "new", :contact_id => @talk.contact} %> |
<%= link_to 'Přehled hovorů', talks_path %>

<p>
Výpis všech hovorů s <%=h  @talk.contact.last_name if @talk.contact   %>
<%= render :partial => 'list' %> 
</ p>